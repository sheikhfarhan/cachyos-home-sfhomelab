##################################################
# ARCANE
##################################################
networks:
  dockerapps-net:
    external: true

services:

  arcane:
    image: ghcr.io/getarcaneapp/arcane:latest
    container_name: arcane
    hostname: arcane
    networks:
      dockerapps-net:
        ipv4_address: 172.20.0.17
        ipv6_address: fd00:dead:beef:2::17
    ports:
      # Allows secure access via NetBird mesh
      - "10.80.0.1:3552:3552"
      - "[fd30:8fa7:ade1:f2e5:13a7:23c:717c:a555]:3552:3552" # IPv6 Access
      # Allows local access via CachyOS terminal or SSH tunnel
      - "127.0.0.1:3552:3552"
    extra_hosts:
      # Intercept Gotify traffic and route it directly over NetBird!
      - "gotify.${ROOT_DOMAIN}:10.80.0.2"
    volumes:
      - /home/sfarhan/homelab/cachyos-home/mon-stack/arcane/arcane-data:/app/data
    environment:
      - APP_URL=http://10.80.0.1:3552
      - DOCKER_HOST=tcp://172.20.0.28:2375
      - PUID=${PUID}
      - PGID=${PGID}
      - ENCRYPTION_KEY=${ARCANE_KEY}
      - JWT_SECRET=${ARCANE_JWT_SECRET}
#    healthcheck:
#      test: ['CMD-SHELL', 'curl -fsS http://localhost:3552/api/health >/dev/null || exit 1']
#      interval: 30s
#      timeout: 5s
#      retries: 5
#      start_period: 20s
    restart: unless-stopped